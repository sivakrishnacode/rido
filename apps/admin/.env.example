# Copy to .env.local for `npm run dev -w @rido/admin`.
# Rido API base URL (server side only; the browser never calls the API directly).
API_URL=http://localhost:3000/v1
# Browser key for the Google Maps JavaScript API (admin maps). Restrict by HTTP referrer in production.
NEXT_PUBLIC_GOOGLE_MAPS_BROWSER_KEY=
